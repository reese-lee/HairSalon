<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Big Dreams, Big Hair Salon</title>
    <link rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css'>
    <link rel='stylesheet' href='~/css/styles.css' type="text/css">
  </head>
  <body class="container">
    <h1 class="headline">Big Dreams, <br> Big Hair Salon</h1>
    <h3>Add a new client for <b>@Model.Name</b></h3>
    <form action="/stylists/@Model.Id/clients/new" method="post">
      <input id="stylistId" name="stylistId" type="hidden" value="@Model.Id">
      <label for="clientName">Add client name:</label>
      <input id="clientName" name="clientName" type="text">
      <button type="submit" class="btn btn-dark" id="buttons">Add this awesome client</button>
    </form>

    <button type="button" class="btn btn-dark" id="buttons"><a href="/stylists/@Model.Id">Back to @Model.Name</a></button>
    <button type="button" class="btn btn-dark" id="buttons"><a href="/stylists">All stylists</a></button>

  </body>
</html>
<!-- for new client under stylist -->
